var tdg = require('tabledatagateway.js');

var personID = 4;

var simpleOnSuccess =  function(data){
  res.status(200).json({
    status: 'success',
    data: data,
  });
  console.log("Daten zurückgesendet.");
};

var simpleOnError = function(err){
  return next(err);
};

exports.getVersicherungOf = function (req, res, next) {
  tdg.selectVersicherungOf(personID,
    function(data){
      res.status(200).json({
        status: 'success',
        data: data,
      });
    },
    function(err){return next(err)}
  );
};

exports.getVersicherung = function (req, res, next) {
  var versicherungID = req.params.versicherungID;
  tdg.selectVersicherung(versicherungID,
    function(data){
      res.status(200).json({
        status: 'success',
        data: data,
      });
    },
    function(err){return next(err)}
  );
}
